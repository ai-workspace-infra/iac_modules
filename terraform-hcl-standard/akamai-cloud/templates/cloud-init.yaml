#cloud-config
package_update: true
package_upgrade: true
runcmd:
  - echo "Provisioned by Terraform Akamai Cloud/Linode VPS env" > /etc/motd
  - systemctl enable --now ssh || systemctl enable --now sshd || true
